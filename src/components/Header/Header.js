import {Button} from 'react-bootstrap';
import * as Styled from "./Header.styled.js";

const Header = () => {
   return (
      <Styled.Header>
         <Styled.StyledButton>test</Styled.StyledButton>
      </Styled.Header>
   );
}

export default Header;
